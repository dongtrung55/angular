import { factorial, isPrime } from './utils';

const num1 = 5;
const num2 = 13;

console.log(`Factorial of ${num1}: ${factorial(num1)}`);
console.log(`Is ${num2} prime? ${isPrime(num2) ? 'Yes' : 'No'}`);
